# AI 客服工作台：项目状态与对话交接

> 本文档用于在新对话中恢复项目上下文。开始新对话时，请先让 AI 完整阅读 `.agents/.skills/skill.md` 和本文档，再检查仓库当前代码；不要只根据本文档推测代码现状。

## 更新约定

- 用户说“更新状态”时，AI 需要根据仓库中的真实代码更新本文档。
- 更新前必须先 Review 当前实现，并运行与本阶段相称的检查。
- 只记录已经完成并验证的内容，不把计划中的功能写成已完成。
- “当前唯一任务”始终只保留一个，不向用户展开完整项目路线图。
- 若本文档与代码不一致，以代码和实际运行结果为准，并修正文档。

## 项目目标

通过实现一个结构真实、可持续演进的 AI 客服工作台，系统训练 Vue 3、TypeScript、SSE / HTTP Streaming 和 WebSocket。项目以学习和理解为优先，不追求炫技或一次性堆叠功能。

## 教学约束摘要

- 每轮只布置一个约 20～60 分钟可完成的小任务。
- 除非用户明确说“给我答案”，否则不直接提供完整实现代码。
- 用户提交实现后，先做 Code Review，再决定下一步。
- 实时通信按业务需要逐步引入；前期不封装万能 `useSSE`、`useWebSocket` 或复杂架构。
- 完整规则以 `.agents/.skills/skill.md` 为准，必须完整阅读该文件。

## 当前技术与环境

- Vue 3 + TypeScript + Vite
- 样式预处理器：Sass `1.104.1`；Vue 组件使用 `<style scoped lang="scss">`。
- Vue Router 已安装并在 `src/main.ts` 注册，目前没有路由记录。
- Pinia 已安装并在 `src/main.ts` 注册，当前只有模板自带的 `src/stores/counter.ts`，业务尚未使用 Pinia。
- 包管理器：pnpm；依赖已经安装。
- Node.js：`v22.20.0`，满足项目的 engines 要求。
- PowerShell 会阻止执行 `pnpm.ps1`，在当前电脑上应使用 `pnpm.cmd`，例如 `pnpm.cmd dev`。

## 已完成并验证

- Vue 项目脚手架已经创建。
- 开发服务器已验证可启动，默认地址为 `http://localhost:5173/`。
- `src/App.vue` 已加入静态三栏语义结构：左侧会话列表、中间当前会话、右侧客户信息。
- 左侧已经使用 `ConversationSummary` 类型和 3 条本地模拟数据渲染会话摘要。
- 每条摘要展示客户名称、最后一条消息、更新时间；未读数量大于 0 时才显示未读徽标。
- 会话列表使用一个直接位于会话面板下的滚动内容容器，单条会话使用独立卡片结构。
- `src/App.vue` 已完成桌面端三栏 Grid 布局：左右栏采用受限弹性宽度，中间栏占据剩余空间。
- 已处理全局盒模型、页面默认边距、视口高度、面板边界以及各面板独立滚动所需的尺寸约束。
- `src/App.vue` 的样式已迁移为 SCSS：重复颜色使用变量，面板标题、内容区和会话卡片使用适度嵌套表达结构。
- 2026-09-22 最近一次状态核对中，`App.vue` 与 `package.json` 的 Prettier 检查、Vue TypeScript 类型检查及 Vite 生产构建均已通过。
- 尚未实现会话选择、消息历史、后端接口或实时通信。

## 当前工作区状态

- 当前改动尚未提交到 Git。
- 已修改：`package.json`、`pnpm-lock.yaml`、`src/App.vue`。
- 新增且尚未跟踪：`PROJECT_STATUS.md`。
- `src/router/index.ts` 仍无业务路由；`src/stores/counter.ts` 仍是脚手架示例，业务尚未使用。
- 状态更新时未发现需要修复的格式、类型或构建错误。

## 当前唯一任务（暂停后从这里恢复）

在 `src/App.vue` 中实现“当前选中了哪一个会话”。

任务边界：

- 实现前，用户先决定选中状态保存“会话 ID”还是“整个会话对象”，并说明理由。
- 点击任意会话卡片后，该卡片需要呈现明确的选中样式。
- 中间栏需要从初始提示切换为能够辨认当前客户的内容；点击其他卡片时同步变化。
- 选中状态需要是 Vue 响应式状态，不能通过手动查询或直接修改 DOM 实现。
- 暂时不要添加消息历史、客户详情、Pinia、路由、后端接口、SSE 或 WebSocket。
- 由用户亲自实现；AI 不应提前给出完整代码。

## 当前任务的学习重点

- `ref` 表达会变化的选中状态。
- 区分源状态与可以从源状态推导出的数据，必要时使用 `computed`。
- 事件绑定、条件渲染和动态 class。

## 当前任务完成标准

- 初始状态下没有会话被选中，中间栏保留选择提示。
- 点击一条会话后，只有该卡片显示选中样式，中间栏能辨认对应客户。
- 连续点击不同会话时，选中样式和中间栏内容都能正确切换。
- 刷新页面后回到未选择状态，终端和浏览器控制台没有报错。
- `pnpm.cmd build` 可以通过。

## 下一次 Review 需要关注

- 选中状态保存 ID 还是对象，以及选择理由是否与当前数据所有权一致。
- 是否只保留必要的源状态，没有重复保存可推导数据。
- `ref` 类型、点击事件和动态 class 是否正确。
- 是否严格控制在会话选择范围，没有提前加入消息历史或全局 Store。

## 状态更新时间

- 2026-09-22：创建交接文档；开发环境已验证；布置第一个业务实现任务（静态三栏工作台骨架）。
- 2026-09-22：按用户要求完成三栏 HTML 语义骨架；构建通过；当前任务收敛为完成三栏布局样式。
- 2026-09-22：按用户要求完成三栏 Grid 样式；格式与构建检查通过；当前任务推进为带类型的会话摘要列表渲染。
- 2026-09-22：按用户要求补全会话摘要列表、未读条件展示和列表容器结构；格式与构建检查通过；当前任务推进为会话选择状态。
- 2026-09-22：按用户要求暂停业务任务，安装 Sass 并将 `App.vue` 的平铺 CSS 重构为 scoped SCSS；格式与构建检查通过；恢复时继续会话选择状态任务。
- 2026-09-22：用户触发“更新状态”；已重新核对源码、依赖和 Git 工作区，并再次通过 Prettier、类型检查及生产构建；暂停点保持为会话选择状态任务。
